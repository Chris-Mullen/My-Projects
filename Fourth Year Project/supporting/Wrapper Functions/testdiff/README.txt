Chris Mullen 10387763

to run (in octave) type "testdiff", this will read two images, "Before.png" and "After.png" and check their average pixel difference, if it is zero, there is
no data loss, it will also output "diff.png" which is the difference of each image subtracted pixel by pixel 